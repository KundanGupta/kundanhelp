@model PropertyPortal.tblpropertycategory

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div class="container-fluid">
    <section class="container">
        <div class="container-page">
            <div class="col-md-6">
                <table class="table table-bordered">
                    <tr class="active">
                        <th colspan="2">Property Category Details
                        </th>
                    </tr>
                    <tr>
                        <td>
                            @Html.DisplayNameFor(model => model.CategoryName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => model.CategoryName)
                        </td>
                    </tr>
                    <tr>
                        <td colspan="2">@Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |   @Html.ActionLink("Back to List", "Index")
                        </td>
                    </tr>
                </table>
            </div>
        </div>
    </section>
</div>